require("options") -- regular vim options
require("plugins") -- plugins with packer
require("lsp") -- language server protocol
require("keymaps") -- regular vim keymap
require("style") -- which style I  currently use
require("rust_lsp") -- special lsp for rust lang
require("navigation") -- nvim tree
require("treesitter") -- syntax highlight & more
require("statusbar") -- the status bar You see below
require("start") -- startpage
-- require("saga") -- some additional def, hits etc
